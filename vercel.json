{
  "version": 2,
  "buildCommand": "npm install && npm run build --workspace=packages/types && cd client && npm run build -- --configuration production",
  "outputDirectory": "client/dist/client/browser",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.ts": { "maxDuration": 30 }
  }
}